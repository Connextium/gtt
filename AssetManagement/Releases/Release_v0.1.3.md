# Release v0.1.3

Date: 2026-07-29

## Summary

Incremental release note publication for v0.1.3.

## Included

- Added release artifact for version `v0.1.3`.
- Prepared repository release metadata for commit/tag publication flow.

## Verification

- `git status` reviewed before release commit.
- `git push origin main` completed after commit.
- `git push origin v0.1.3` completed after tag creation.

## Notes

- No git submodules are configured in this repository (`git submodule status` returned no entries).
- Existing working tree changes outside this release note were intentionally preserved.
